services:
  - type: web
    name: whisper-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn whisper_backend:app --host 0.0.0.0 --port 8000
    plan: free
